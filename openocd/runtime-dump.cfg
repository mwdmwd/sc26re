if { ![info exists STOCK_DUMP_DIR] } {
	error "STOCK_DUMP_DIR is required"
}

init
halt

echo "CPU registers:"
reg

echo "dumping RAM"
dump_image [file join $STOCK_DUMP_DIR ram.bin] 0x20000000 0x00020000

resume
shutdown
